# 知识库数据（未包含在本仓库）

本目录存放说明书知识库（`sections_raw.jsonl` / `slice_map.jsonl` / `catalog_locked.json` / `images/` 2608 张插图等）。

其中的产品手册与插图为第三方材料，出于版权考虑**不随仓库分发**。
复现时请将数据放入本目录，并按根 `README.md` 与 `.env.example` 配置后运行。
